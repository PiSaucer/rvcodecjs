{
  "compilerOptions": {
    "incremental": true, /* Enables incremental compilation so that only source files
        that change between compilations are recompiled. */
    "target": "es5", /* Target is the version of Javascript the TS compiler (tcs)
        will output and decides what browsers our website will work on. 
        ES5 is a version of Javascript that was well supported by browsers around 2013. */
    "sourceMap": true, /* Enables emitting source maps. Source maps are like debug symbols
        for Javascript compilers/debuggers. */
    "outDir": "./out/", /* Directory where tsc emits the final .js files. */
    "strict": true, /* Enables a set of options that use the type system to issue more
        warnings and errors at compile time. */
    "forceConsistentCasingInFileNames": true /* Makes tsc issue an error if import 
        statements have different casing from file name on disk for consistency between 
        filesystems that treat casing in filenames differently. */
  },
  "include": [
    "core/*",
    "tests/test_runner.ts"
  ]
}
